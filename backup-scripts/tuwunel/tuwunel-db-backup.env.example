# Copy this file to: tuwunel-db-backup.env (same folder) and fill in values.
# That real file will be ignored by git.

# Matrix homeserver base URL
HOMESERVER="https://matrix.dariush.dev"

# Target room and token (secret)
ROOM_ID="!yourRoomId:yourServer"
TOKEN="PASTE_YOUR_LONG_LIVED_ACCESS_TOKEN_HERE"

# Optional: command/message to trigger the backup bot
MESSAGE="!admin server backup-database"
